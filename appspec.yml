version: 0.0
os: linux

hooks:
  ApplicationStop:
    - location: WebApplication/scripts/stop_container.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: WebApplication/scripts/start_container.sh
      timeout: 300
      runas: root